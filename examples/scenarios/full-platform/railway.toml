[build]
builder = "NIXPACKS"
buildCommand = "npm install && npm run build -w @perch-brief/api"
startCommand = "npm run start -w @perch-brief/api"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
