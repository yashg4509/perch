name: render
category: hosting
deployable: false

api:
  base_url: "https://api.render.com"
  auth_header: "Authorization: Bearer {token}"
  endpoints:
    status: "GET /v1/services"

credentials:
  key: render_api_key
  prompt: "Render API key (dashboard.render.com)"
